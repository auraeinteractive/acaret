// Ai tools

window.toolbar = window.toolbar ? window.toolbar : {};
window.toolbar[ 'ai-tools' ] = function() {
    let topToolbar = document.getElementById( 'top_toolbar' );
    topToolbar.innerHTML = '<div><strong>Artificial Intelligence tools</strong></div>';
}
